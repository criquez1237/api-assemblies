meta {
  name: Add Specification
  type: http
  seq: 4
}

post {
  url: http://localhost:8081/api/products/a791f32c-a6a5-45ad-b3d5-3b97e2bdbc81/specification
  body: json
  auth: bearer
}

auth:bearer {
  token: <YOUR_JWT_TOKEN>
}

body:json {
  {
    "id": "spec2",
    "name": "Batería",
    "value": "5000mAh",
    "visible": true,
    "actived": true,
    "deleted": false,
    "createdAt": "2025-07-21T10:00:00.000Z",
    "updatedAt": "2025-07-21T10:00:00.000Z",
    "deletedAt": null
  }
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has success message", function() {
    expect(res.getBody().message).to.contain("Especificación agregada");
  });
}
