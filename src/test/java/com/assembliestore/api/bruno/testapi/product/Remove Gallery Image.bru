meta {
  name: Remove Gallery Image
  type: http
  seq: 2
}

delete {
  url: http://localhost:8081/api/products/a791f32c-a6a5-45ad-b3d5-3b97e2bdbc81/gallery/img2
  auth: bearer
}

auth:bearer {
  token: <YOUR_JWT_TOKEN>
}

tests {
  test("Response status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("Response has success message", function() {
    expect(res.getBody().message).to.contain("Imagen eliminada");
  });
}
